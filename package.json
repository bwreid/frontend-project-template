{
  "name": "project",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ],
      [
        "scssify"
      ]
    ]
  },
  "scripts": {
    "start": "watchify ./scripts/app.js -o ./scripts/bundle.js & live-server .",
    "build": "node build.js",
    "deploy": "npm run build && surge -p ./dist -d g67-gtools.surge.sh"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "fs-extra": "^4.0.2",
    "live-server": "^1.2.0",
    "scssify": "^2.3.0",
    "surge": "^0.19.0",
    "watchify": "^3.9.0"
  },
  "dependencies": {
    "bulma": "^0.5.3"
  }
}
